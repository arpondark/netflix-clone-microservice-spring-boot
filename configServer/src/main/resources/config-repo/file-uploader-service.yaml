server:
  port: 8082

spring:
  servlet:
    multipart:
      max-file-size: 10GB
      max-request-size: 100MB

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

file:
  upload-dir: uploads
